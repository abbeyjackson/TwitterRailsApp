<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @tweet.title %>
</p>
<p>
  <strong>Body:</strong>
  <%= @tweet.body %>
</p>

<p>
  <strong>User:</strong>
  <%= @tweet.user_id %>
</p>

<%= link_to 'Edit', edit_user_tweet_path(@user, @tweet) %> |
<%= link_to 'Destroy', [@tweet.user, @tweet], method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Back', user_tweets_path(@user) %>
